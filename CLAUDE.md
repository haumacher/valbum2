# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

VAlbum2 — a self-hosted photo/video album. A Java backend (Jetty) serves albums read from a folder tree as a JSON API; it never modifies or deletes the original photos (all edits are stored in sidecar files; the only file operations on originals are same-filesystem renames — the explicit library migration, the album move of #47, which sets duplicates aside in `<space>/.valbum/duplicates/`, and the delete of #109, which renames an album holding photographs into `<space>/.valbum/trash/`). The only front end is the Flutter app in **`valbum_ui/`** (web, mobile, desktop). The former GWT web client was removed in ROADMAP Phase 0.

Direction, phases and decisions are recorded in `ROADMAP.md`.

## Two separate toolchains

This repo mixes **Maven** (Java backend, multi-module: `image-server`, `image-server-shared`, `util-servlet`) and **Flutter/Dart** (`valbum_ui/`), with independent dependency management. A full build means running both.

### Maven (backend)

- Build everything: `mvn clean install` (run from repo root)
- Run the demo server: `mvn exec:java@test-server -pl :image-server` → http://localhost:9090/valbum/ (port 9090, non-standard)
- Run the packaged jar: `java -jar image-server/target/image-server-jar-with-dependencies.jar --basepath /path/to/photos [--port 8080] [--contextpath valbum] [--webroot /path/to/flutter/build/web] [--auth off|writes|all] [--admin-code <code>] [--invite admin|members] [--preview-threads <n>] [--migrate-to-user <name>]`
- Authentication and permissions (#28, #45, #82, #83): `--auth` (`off|writes|all`) says what an anonymous caller may do; a device pairs with `POST <data>/?action=pair` carrying a **code** (#89: one single-use secret, ten minutes, one device, one target user — the seat code the server prints, a code of one's own device, a recovery code from an administrator) and then sends `Authorization: Bearer <token>`. **A space is the unit of everything**: one server hosts one space (the base folder, today's addresses) or several (#82). A user belongs to exactly one space, lives in that space's `<space>/.valbum/users.json` with their devices, and holds **one permission for the whole space**: `{role: admin|edit|contribute|view, clearance: public|nonPrivate|all, mayShare: bool}` (#83). `AuthService.rights(caller, path)` answers from the role alone and the same for every path — admin/edit → view+download+contribute+edit, contribute → view+download+contribute, view → view+download; a share link answers what the link itself was created with; an anonymous caller answers view+download where the space is open (`--auth writes`, or `space.json` `anonymous: public`) and nothing otherwise. `AuthService.clearance(...)` answers the stored clearance (admin: everything; anonymous: public), `?viewAs=public|members` lowers one's own for a request, and `PrivacyFilter` drops what is above it on the way out (`ImagePart.privacy` 0..2, cache and sidecars untouched). Every endpoint asks those two: listing/thumbnail → `view`, original → `download`, sidecar PUT / move source / `?action=place` → `edit`, upload / `?action=check` / move target / album creation → `contribute`. `Roles.of` reads the older names of a library written before #83 (`member` → edit/all/share, `guest` → view/nonPrivate/no-share) without rewriting the file, and the protocol answers the new names. Management (admin only): `GET <data>/?type=users` (role, clearance, mayShare, devices), `POST <data>/?action=invite|uninvite` (the invitation carries role/clearance/mayShare, capped at the inviter's), `?action=set-permission` (`UserPermission{name, role, clearance, mayShare}`; the last admin is never demoted, 409 `LAST_ADMIN`), `?action=remove-user` (`MemberName`; their devices go, their photos and their attribution stay). **Retired by #83, answering 410 with an `ErrorInfo` naming the replacement for one release**: `?type=grants|groups|links`, `?action=grant|revoke|group|ungroup|regroup|unlink|promote` — grants, groups, materialised links, guest roots and the `~<owner>/` path form are gone, with their stores (`GrantStore`, `GroupStore`, `Subjects`, `links/*`). Moving (#47): `POST <folder>/?action=move` with a `MoveRequest` answers a `MoveResult` per name; `MoveService` renames only, carries the `ImagePart` and its `.hashes.json` entry along, sets duplicates aside; a move out of an album needs `edit` or the caller as contributor of every named image (`CONTRIBUTION_REFUSED`); the kind of the target decides (#113) — an image whose target is a folder of folders is refused `notAnAlbum(name)` and an album or folder whose target is an album `notAFolder(name)`, both before anything is renamed. Deleting (#109) is that move with the trash as its target: `POST <parent>/?action=delete` with the same `MoveRequest` (`target` empty and ignored) answers the same `MoveResult`, needs **`edit`** on the parent (a share link 403 `SHARE_DELETE_REFUSED`, the contributor exception of #53 does not reach a whole album), and acts on **albums and folders only** (an image is taken out by moving it, `DeleteService.notAnEntry`). `DeleteService` decides per name: a folder that holds **no picture anywhere below it** — `ResourceCache.isImage`, a photo rated −2 counting as a picture, a file in `.vacache` counting unless `CacheRefresh.isGenerated` names it — is **removed physically**, and then only what the server itself wrote (`index.json`, its `index.json.<ts>` backups, `.hashes.json`, everything inside `.vacache`) plus the emptied directories; one foreign file anywhere below refuses the removal and the folder goes to the trash instead, files and all (`DeleteService.removable` computes the whole list before anything is touched, pinned by `TestImageServletDelete.testNothingButWhatTheServerWroteIsEverDeleted`). Everything else is **one rename** into `<space>/.valbum/trash/<name>`, sidecars and hashes riding along, previews abandoned; a name the trash already holds gets the prefix `<yyyyMMdd-HHmmss>-<name>` with `-2`, `-3`… behind the whole name (`ImageServlet.freeName` numbers before a file extension and a folder has none). The outcome names which happened — `newName` the name in the trash or `""` for a removal, `message` `REMOVED` or `trashed(name)`. Emptying the trash is the owner's act, never the server's, and **`AuthService.walk` now refuses any path segment starting with `.`** (404 `PATH_ESCAPED`), so no address reaches into `.valbum` — the trash, the duplicates and the user store alike. The **duplicates folder moved per space** in the same change: `MoveService.setAside` writes below `PathInfo.getBasePath()`, the caller's space root, which in single mode *is* the base folder (unchanged paths); what an older multi-space server left below the server base stays there and is named once at start-up (`DeleteService.legacyDuplicates`, printed by `Main.start`). In the app the delete is one shared flow (`move_view.dart` `deleteWithConfirmation`, `client.dart` `delete(...)`): "Delete…" in the listing tile's long-press menu (key `delete-entry`, offered exactly where "Move to…" is) and "Delete album…" in the album menu (key `delete-album`, offered where "Move album to…" is), both refused offline, never in a share session, confirming with `deleteExplanation` — "An album without any image is removed; one with images is moved to the trash folder of the space (nothing is deleted from disk)." — showing the server's own outcome message afterwards and, for the album one is standing in, ascending to the parent as `moveAlbum` does. **The hashes of a space are indexed** (#118): `HashIndex` (`<space>/.valbum/hash-index.json`, `{version, folders: {<path>: {stamp: <mtime of its .hashes.json>, hashes: {<sha256>: <file name>}}}}`) is a lookup of hash → path relative to the space root, **derived** from the per-folder `.hashes.json` sidecars, which stay the stored truth — no database, one map. One index and one low-priority single-thread executor per space, started by `Main.createServer` (`ImageServlet.startIndexing()`, never by a test) and shut down in `destroy()`: the first pass reads the tree, skips every folder whose sidecar stamp is unchanged, and **hashes every folder whose sidecar does not name all its images** (`HashCache.refresh()` + `flush()`, so the sidecar is written and a restart finds it done), resumable, the index file written atomically at most every `WRITE_INTERVAL_MILLIS` (2 s) and always at the end, never on the request path. It is kept current by the one writer of a sidecar — `HashCache.flush()` calls `HashIndex.sidecarWritten(folder)`, which covers the upload, the move and the lazy fill of a check — plus `treeChanged(folder)` after a move or a delete (a folder rename announces nothing), and `pathOf` checks that the file it found is still there, so a stale entry is forgotten rather than answered. `?action=check` therefore answers **space-wide**: the target folder first (a bare name, as before), then the index (`PresentFile.name` becomes a path — `2020/Trip/IMG_1.jpg` — which the app only shows), plus `UploadCheckResult.indexed` (`IndexProgress{done, total}` in folders, `done == total` only when complete, `total = done + 1` while nothing is known); a share-link caller is confined to its folder and told nothing of the index. The sweep is `POST <album>/?action=find-duplicates` (needs `edit`, not admin; a share link and anybody else 403 `DUPLICATES_REFUSED`): `MoveService.setAsideDuplicates` hashes the album and renames every photo whose hash the index finds in **another** folder of the space into `<space>/.valbum/duplicates/` by the #47 mechanism, takes the part out of the sidecar and answers a `MoveResult` naming where the copy that stays is (two copies inside the one album are not duplicates of the space and stay). Share links (#51, #84): `POST <folder>/?action=share` with a `ShareLink` (label, expires, maxPrivacy, minRating, rights) needs the **share flag** (`AuthService.mayShareLinks`, else 403 `SHARING_REFUSED`); `edit` in the rights is 400 `SHARE_EDIT_REFUSED` and a `maxPrivacy` above the creator's own clearance is 400 `shareAboveClearance(...)` — **refused, never quietly trimmed**, so nobody hands out a link they believe shows less than it does. What the link may do and show is written on it (`{id, tokenHash, owner, path, rights, maxPrivacy, minRating, createdBy, created, revoked}` in `<space>/.valbum/shares.json`) and **frozen there**: a later change to its maker's permission neither widens nor narrows it. **The link is the permission — there is no grant beside it.** The answer is `ShareLinkCreated` (token once, `url` = `<context>[/<space>]/s/<token>/`); **a link posted in a messenger shows a card** (#104, `SharePreview`, installed on `ResourceServlet` as the `PageDecorator` and the `SessionResource` beside #88's `SessionGuard`): the page load of a share base only — never an invitation's, never a dead link's — carries `<title>`, `og:title` (the shared album's title, never the link's label), `og:description` (its subtitle, failing that its date), `og:url`, `og:image` and `twitter:card`, HTML-escaped and nothing stored, and the one picture they name is `<context>[/<space>]/s/<token>/cover.jpg` — the `PreviewCache` rendition of the album's index picture (of the first child that has one, for a shared folder), answered **anonymously**, the token in the path being the authority for that one resource and nothing else, with the link's own `min(MEMBERS, maxPrivacy)` and `minRating` through `PrivacyFilter` (an index picture the link may not show falls back to the first image it may, none at all → 404 and no `og:image`), expired/revoked/unknown → 410 with an `ErrorInfo`, `Cache-Control: no-store` and `Vary: Authorization` like every session answer. A share bearer's `/` is the shared folder (outside → 404 `SHARE_CONFINED`), it reads even where the space refuses anonymous callers, contributes only with `contribute` and is attributed `token:<id>`, clearance `min(MEMBERS, maxPrivacy)` (a link never shows a private image, whoever made it), parts below `minRating` dropped, expired/revoked → 410 on **every** endpoint including thumbnails and renditions, `AuthInfo.share` set for link callers only; a token of another space is simply unknown there. Management: `GET <folder>/?type=shares` and `POST ?action=unshare` show and withdraw **one's own** links, an administrator's every link of the space (`ImageServlet.mine`; a link of somebody else's is answered `SHARE_UNKNOWN`, never "whose"); `?action=remove-user` withdraws every link the removed user handed out and says how many in `UserList.revokedLinks`. Invitations (#52, #89): **an invitation is a pending user carrying a code — there is no invitation store any more.** `POST <data>/?action=invite` (body `Invitation{role, clearance, mayShare, note, expires, recipient}`) creates a **pending user** in `<space>/.valbum/users.json` — `name: ""`, no devices, the invitation's permission (capped at the inviter's), plus `invitation` (the code's id), `invitedBy`, `note` and the optional **recipient memento** — and **one code** in `<space>/.valbum/device-codes.json` with `kind: "invitation"`, 32 random bytes base64url, the invitation's expiry (days), exempt from `DEVICE_CODE_ISSUER_GONE` like the backup code; the answer is `InvitationCreated` with the URL `<context>[/<space>]/i/<token>/`. `DeviceCodeStore.lookup` matches an invitation record against the hash of the token **as it stands** and every other record against the normalised typed form, so the two spellings live in one store and a token hashed by the old `InvitationStore` still resolves. Redeeming is the **ordinary pairing**: `PairRequest{deviceCode, userName, deviceName}` → `addDevice` → the pending user is nameless → `NAME_REQUIRED` unless a free `userName` is given → named, first device added, code used (`PairRequest.invitation` is read as an alias of `deviceCode` for one release). `?type=invitations` is **derived** (`AuthService.invitations()` joins pending/invited users with their codes); `?action=uninvite {id}` revokes the code and **removes the pending user if nobody joined** (a named user stays); `?action=remove-user` also withdraws the unaccepted invitations that user sent; an expired invitation's pending user is swept at start-up and before every listing (`AuthService.pruneInvitations`); a pending user never counts for `LAST_ADMIN` and is never named by #86's start-up naming. `?type=users` lists pending users with `UserEntry{pending, recipient, invitedBy, invitation}` (the app shows "Invited for … (pending)" with *Withdraw*); the memento stays beside the name once somebody joined and is **never shown to the invitee**. Dead invitation codes are kept `KEEP_DEAD_INVITATION_DAYS` (30) instead of 24 hours, so #88's reasons survive a month. An `invitations.json` written before this build is carried over once at start-up (`AuthService.adoptInvitations`): every **live** invitation becomes a pending user + a code with the **same token hash**, the dead ones are dropped and the file is moved to `.valbum/retired/<timestamp>/`. An invitation bearer is anonymous everywhere but `?type=auth`, which answers `AuthInfo.invitation` (`InvitationInfo{role, invitedBy, note, expires, recipient}`); only a `kind: "invitation"` code is ever a bearer — a typed device code opens nothing; a **page load** of a dead invitation address (used, expired, withdrawn, unknown to that space) is answered `302` to the space's app base with `?invitation=used|expired|withdrawn|unknown` (#88, `InvitationRedirect` wired into `ResourceServlet.setSessionGuard`; assets and `/s/<token>/` untouched, the record never written). Device codes (#65, #89): `POST <data>/?action=device-code` answers `DeviceCodeCreated{code, expires}` (eight characters of `ABCDEFGHJKLMNPQRSTUVWXYZ23456789`, ten minutes, once) stored hashed in `<space>/.valbum/device-codes.json`; it is never a bearer and never a URL, only `PairRequest.deviceCode`, and it dies with the device that issued it (`DEVICE_CODE_ISSUER_GONE`, checked against every device of the space since #89, because an administrator's device may issue for somebody else). The body is an optional `DeviceCodeRequest{userName}`: empty means "for myself" as before, a name is the **recovery code** an administrator makes for an existing user who lost every device (a non-admin naming somebody else → 403 `RECOVERY_REFUSED`, an unknown user → 404 `unknownUser`, an unreadable body → 400 `DEVICE_CODE_UNREADABLE`). Backup code (#92): `POST <data>/?action=backup-code` answers a `DeviceCodeCreated` for the caller themselves — the same record in the same store, told apart by a `kind: "backup"` field (absent/empty = an ordinary code, so an old `device-codes.json` reads unchanged), **16** characters of the same alphabet (80 bits, shown `XXXX-XXXX-XXXX-XXXX`; `DeviceCodeStore.format` groups by four), **`expires` empty = never** (`isExpired` reads an empty expiry as "never" only for a backup code; on any other record it still reads as expired), exempt from `DEVICE_CODE_ISSUER_GONE` and from `revokeIssuedBy` (it is made on the very device one is about to sign out of), single use, **one per user** (creating withdraws the earlier one, which is then refused `BACKUP_CODE_REVOKED`), redeemed through the ordinary pairing in the one code field (the store looks up by hash; the length says nothing). `POST ?action=revoke-backup-code` withdraws it (404 `NO_BACKUP_CODE` where there is none) and answers the device list; a share link is refused 403 `BACKUP_CODE_REFUSED`, an anonymous caller 401. `DeviceList.backupCodeCreated` says whether there is one and since when — never the code. Pruning after `KEEP_DEAD_HOURS` never touches a live backup code. Devices: `GET <data>/?type=devices` answers one's **own** devices, `POST ?action=unpair` removes one of one's own (and revokes the codes it handed out, the backup code excepted). Token lookup: devices, then shares, then invitation codes. `GET <data>/?type=auth` reports mode, device, user, **role, clearance, mayShare**, space, and the share or invitation a token opened. Every JSON answer carries `Cache-Control: no-store` and `Vary: Authorization` (`ImageServlet.allowCrossOrigin`). Attribution (#53): `.hashes.json` entries carry `contributor` (`user:<name>`, `token:<id>`, `anonymous`) and `contributorLabel`, derived onto `ImagePart.contributor`/`contributorLabel` on read and cleared before any sidecar write. Cameras (#78): `ImagePart.camera` is `ImageData.cameraLabel(make, model)` from the EXIF `Make`/`Model` (both trimmed, blanks collapsed, joined with one blank, the make left out when the model already begins with it as a whole word ignoring case), stored in the sidecar like `date`. Positions (#112): `ImagePart.location` is an optional `GeoLocation{latitude, longitude}` in decimal degrees, read once at analysis from the EXIF GPS IFD (`GpsDirectory.getGeoLocation()`, which applies the N/S/E/W reference letters and also answers an mp4 or QuickTime container's own position) and **stored in the sidecar** like `camera` — absent means the file says nowhere, `0/0` stays a real place, and a part a sidecar already lists is never analysed again; it follows the image's own privacy level and nothing besides, so whoever may see the image is answered its position (`PrivacyFilter` unchanged). Video dates (#72, #102): a video is dated by the `mvhd` creation time of its container, and a time before `ImageData.EARLIEST_RECORDING` (1990-01-01 UTC) is no recording time; the chain then asks the **file name** — `ImageData.nameDate` reads `YYYYMMDD[_- ]HHMMSS…`, `YYYY-MM-DD[ at |_| |T]HH[.:-]MM[.:-]SS` or a bare day (midnight) in the default zone, plausible only for a year in 1990…next year and a real day and time, the rule pinned for both toolchains by `image-server/src/test/fixtures/name-dates.json` — and only then does the file's modification time stand; `AlbumUtil.insertSorted` never resorts what a sidecar already lists. Dates and placement (#48): `AlbumInfo.date` is stored, `effectiveDate` is derived on read by `AlbumDate` (explicit → folder-name date → earliest image date) and cleared before any sidecar write; `ListingInfo.placement` (`NONE|BY_YEAR|BY_YEAR_MONTH`) is applied by `PlacementRule` on creation, on a move into the folder, and by `POST <folder>/?action=place`. Folder names (#130): a folder's name on disk is derived from its properties and the server keeps it so — `FolderNames.of` composes `yyyy-MM-dd title` for an album (`FolderNames.albumFolderName`, the Java twin of `valbum_ui/lib/album_date.dart`'s `albumFolderName`, both pinned by the shared table `image-server/src/test/fixtures/folder-names.json`) and the title alone for a folder of folders, and **every** sidecar PUT of an existing folder renames the directory to it before writing (`MoveService.renameFolder`, a same-filesystem rename: sidecar, `.hashes.json` and `.vacache` ride along, `ShareStore.rename` rewrites the `path` of every link on the folder and below it). The answer is always a `CreateResult{path, message}` — where it is now, and `FolderNames.renamedTo(...)` when it moved — which the app follows (`showPath`) and says in a snack bar. A name a sibling holds → 409 `nameTaken`, a title composing to no legal name (`.`, `..`, leading `.`, `/`) → 400, **nothing written** in either case. Never renamed: the space root, a year/month folder, and a folder whose properties compose nothing (no title). An album with no explicit date **keeps the date text its name carries** (`AlbumDate.leadingDateText`), so the folder-name date stays the fallback it is (`AlbumDate` unchanged) and writing a description never undates an album; a rename never *places* the album, so a changed date does not file it into another year folder. The date is shown where the album is named (#107): `albumDateLabel` (`valbum_ui/lib/album_date.dart`) writes `effectiveDate` out in the locale's medium form (`DateFormat.yMMMd()`) and answers `null` for `0`, and that one line stands between the title and the subtitle of a listing tile (key `folder-date`) and under the album's own heading (key `album-heading-date`, `album-date` being the properties dialog's field). **A folder of folders has no date** (#133): `FolderInfo.kind` (`FolderKind` `ALBUM|FOLDER`) is derived on every read — the sidecar's type, else whether the folder holds images, the rule `Loader.loadDir` opens it by — carried through `PrivacyFilter`, cleared by `AlbumDate.clearDerived` and never stored; `ALBUM` is the first constant, so a listing of an older server reads as before. `effectiveDate` stays the **sort key** of every entry (a folder named `2026` still sorts with its year), and the app shows the `folder-date` line and the move picker's date only where `folderHasDate` (`album_date.dart`) — an album with a date. **An album may have no date** (#119): the create dialog's date field is optional, the folder name is `albumFolderName(date, title)` — `yyyy-MM-dd title`, the title alone without a date, the one composition the move picker's `folderLine` uses too — and the dialog says "Without a date the album stays in this folder.", which is what the server does (`PlacementRule.placementFor` files nothing that has no date, pinned by `TestPlacement.testAnAlbumWithoutADateStaysWhereItWasAsked`); that is how an `Inbox` is made by hand.
- **Inboxes (#131, #135)**: an inbox is a **kind of album**, not a resource — `AlbumInfo.kind` (`AlbumKind{ALBUM|INBOX}`, stored in `index.json`, absent = album, set and reversed by an ordinary properties PUT), so upload, hashes, move, delete, thumbnails and attribution work there unchanged. `FolderKind` gains `INBOX`, answered by `loadFolderInfo` from the sidecar and cleared by `clearDerived` like the others. **An inbox has no date**: `AlbumDate.ofFolder`/`ofAlbum` answer `NONE` for one, so `effectiveDate` is 0 on the album and its listing entry, `PlacementRule` files it nowhere and `FolderNames.of` names it by its **title alone** (#130 renames `2023-11-14 Inbox` → `Inbox`); it sorts **first** in `ResourceCache.BY_DATE`, ties by name. **Order is the date**: `Inboxes.flatten` answers the parts flat (a group dissolves into its members, a heading is dropped) sorted by `ImagePart.date` — undated last, then by name — on the way out beside `PrivacyFilter` and never in the loader, because `MoveService` writes the sidecar from the cached album; the stored order and groups are untouched, so the album is itself again when the flag is cleared, and because an inbox shows no group, **naming one of its images acts on that image alone** (`Inboxes.groupsTravel`, read once by `MoveService.move` and honoured in `classify`): the member leaves the group the sidecar still remembers, a group left with one member becomes a plain part, and the photograph arrives at its target — an album or the trash — as one plain `ImagePart`; outside an inbox the #47 rule that the representative carries its whole group is unchanged, and the duplicate sweep of #118 always acted per photograph. A sidecar PUT is the other direction: `Inboxes.restoreArrangement` puts the received parts back into the **stored** arrangement whenever an inbox is involved and the received parts name exactly the same images, so a round trip keeps every edit (rotation, time, privacy, comment) and never freezes the derived order — an order or grouping a client sends for an inbox is ignored, which is what "no reorder, no groups" means; a changed set of images is a statement and is stored as it came. `AlbumUtil.insertSorted` and `?action=place` needed no change. **Visibility** (`Inboxes.visibility`, one decision): `edit` → all of it; a paired member with `contribute` → **their own contributions only** (`ImagePart.contributor == Caller.subject()`, the tile keeping its cover only where the cover is theirs); a `view` member, a share link, an anonymous caller and **any `?viewAs` preview** → it does not exist — 404 with an `ErrorInfo` (`Inboxes.NOT_FOUND`) on the album, its images, thumbnails, renditions and originals, the entry dropped from the listing above and from a share card's cover (`SharePreview`), `POST ?action=share` on an inbox 400 `INBOX_NOT_SHARED`. Privacy editing stays and an image leaving keeps its stored level (the author's decision). **`?action=delete` now names images too** (general for every album): each named image is moved into `<space>/.valbum/trash/<album folder>/` by the #47 mechanism (`ImagePart` out of the sidecar, `.hashes.json` entry carried, the trash album's own sidecar written, `freeName` numbering, duplicates set aside), answering `MoveResult` with `trashed(name)`; it needs `edit` — **or**, for a request naming photographs only (`DeleteService.onlyImages`), the caller as contributor of every one of them (`CONTRIBUTION_REFUSED`), the #53 rule reaching no further than it does for a move; a share link is still refused `SHARE_DELETE_REFUSED` and nothing is ever unlinked.
- A folder of folders is shown by a picture too (#110): `ListingInfo.index` names **the child entry** whose picture stands for this folder — a plain name, never a path (`..`, a slash or a leading dot is an address and is ignored) — stored in the folder's own `index.json` beside `placement` and written by the ordinary sidecar PUT; empty means no choice and the folder icon stays, because **a folder's picture comes from an explicit choice only** and the server never guesses one. `FolderCover.of` resolves it at listing time through the sidecars alone — never an image read, the #48 rule — an album passing on its `indexPicture` and a further folder being asked the same question again, at most `FolderCover.MAX_DEPTH` (8) levels deep; what comes out is `FolderInfo.indexPicture` with `ThumbnailInfo.image` carrying the path from the entry down to the photograph (`A/a.jpg`, `G/A/a.jpg`) and the album's own crop and #115 frame, so the address the app builds (`<listing>/<name>/<image>`) is an ordinary image address and `?type=tn` serves it as it serves any other. A chosen child that is gone, has no sidecar, shows no picture or whose file is gone is simply no picture — 200, nothing logged, the choice left standing (moved away and back, the picture returns). `PrivacyFilter.filterEntry` filters a folder's cover in the album the photograph really lies in, and a cover the caller may not see leaves the tile in place **without a substitute** (a folder is shown by the picture its author chose and by no other); `Inboxes.uncovered` does the same for a cover reaching into an inbox, which only a caller with `edit` on it is ever shown (#135), contributors included in the refusal. `SharePreview` draws a shared folder's card with the chosen child before falling back to its first child that has a picture. A rename by #130 is carried into the choice in the same step (`MoveService.followRename`, the one folder directly above and only where it named exactly this child, a failed write leaving a dangling name and no error); a move of the chosen child away is not chased and simply resolves to no picture. `AlbumDate.clearDerived` now drops the whole `folders` list of a stored `ListingInfo` — every entry is rebuilt from disk on read, and a frozen cover would keep a choice living one folder below — while `index`, the title and the placement rule stay, and the field rides along with every copy of a listing (`PrivacyFilter`, `Inboxes`, `ImageServlet.withRights`).
- Faces (#124, Phase 7): a space with `faces: on` in its `<space>/.valbum/space.json` (`SpaceStore`, missing/unknown → off; `?type=auth` answers `AuthInfo.faces`, false too where the machine cannot load the detector) gets a **face index** — `FaceIndex` per space, started by `ImageServlet.startIndexing()` (Main only, the #118 rule) and shut down in `destroy()`: one low-priority single-thread daemon executor, one photograph at a time, independent of the #69 permits and of #74's transcoder, queued when a listed album reports itself pending (*after* the answer is built, so what it says is what it was built from) plus one start-up walk. The input is the **preview** of `PreviewCache`, made through the ordinary path (permit and all) when missing, never the original; **photographs only** (a video is never handed to the detector, #123), a face under 40 px on the preview skipped, a refused photograph remembered for the process lifetime like a failed transcode (`FaceIndex.forget`, cleared by `?action=refresh-cache`). The runtime is `FaceDetection`: OpenCV through the `org.bytedeco:opencv-platform` the video path already brings in (now declared explicitly, `${opencv.version}` = `4.6.0-1.5.8`) and the **official OpenCV Java bindings** (`org.opencv.objdetect.FaceDetectorYN`/`FaceRecognizerSF` via `Loader.load(opencv_java.class)` — the 1.5.8 presets wrap neither), with two ONNX models bundled beside it and unpacked once into the temporary file space (`face_detection_yunet_2022mar.onnx`, MIT, 345 KB — **not** 2023mar, whose graph needs an OpenCV DNN ≥ 4.8 — and `face_recognition_sface_2021dec.onnx`, Apache-2.0, 38.7 MB; sources, hashes and licences in `NOTICE.txt` beside them; the int8 SFace, 9.9 MB, loads and separates the fixture nearly as well and is the swap to make if the package size matters); `FaceDetection.unavailability()` is asked once like `VideoRenditions.unavailability()`, and a machine that cannot load it prints one line and serves albums without faces, never a crash. The result is **cache**: `<album>/.vacache/faces.json` (`FaceCache`, `{version, model, files: {<sha256>: {faces: [{box, score, embedding}], cluster: [...]}}}`), keyed by the SHA-256 the `.hashes.json` already knows (so a rename or a #47 move finds its faces again and the target album groups them anew, and changed contents simply have no entry) and stamped with `FaceDetection.MODEL` — a stamp that does not match is not read at all, which is how a new model re-detects a library. **The box is normalised 0..1 in the raw raster of the file**, before the EXIF orientation and before `ImagePart.orientation` (`Faces.toRaw`/`toUpright`, the eight orientations as a permutation of the two normalised axes, pinned by `TestFaceGeometry` and on a real rotated file by `TestFaceOrientation`), and because `PreviewCache` never scales up, the box is a fraction of `FaceIndex.content(...)` — the picture's rectangle inside the preview, `min(preview, display)` centred — not of the canvas; a box reaching over the picture is clipped, one entirely in the margin dropped. Clustering is per album (`Clustering`: agglomerative, average linkage on cosine similarity, SFace's own threshold 0.363, groups named `c1`, `c2`… largest first). On the wire `ImagePart.faces` (`repeated FaceInfo {int index; double x, y, w, h; string cluster;}`) and `AlbumInfo.facesPending` (on the album, because #126's editor stands in one), **derived on read and cleared before any sidecar write** (`FaceIndex.clear` from `AlbumDate.clearDerived`) and **added to a per-caller copy, never to the cached album** — `FaceIndex.derive` copies the parts through the model's own reader and writer, so a path that forgets to ask answers none. Who is answered: `Faces.maySee(auth, caller, viewAs)` — signed-in members, plus everybody under `--auth off`; an anonymous visitor of an open space and a **share link never see a face** (#96's spirit), a contributor in an inbox sees only their own photographs' faces because #131 already removed the rest, and the **author's own preview counts**: `?viewAs=public` answers no face and no crop, exactly as the public is answered, while `?viewAs=members` still shows them — the threshold is `Privacy.MEMBERS`, the level a face is answered at, read by the album endpoint and by `?type=face` from the same parameter the privacy filter reads. `GET <image>?type=face&face=<n>` answers a JPEG crop with a quarter-box margin, cut from the preview and cached as `.vacache/face-<name>-<n>.jpg` (`.tmp` + atomic move), needs `view` like the thumbnail, and answers **404 `FACE_NOT_FOUND`** for an unknown index and for every caller answered no faces — never 403, which would say there is something to refuse. `CacheRefresh.isGenerated` knows `faces.json` and `face-*.jpg` (and their `.tmp` names). **Found on the preview, described from the original** (#140): where the preview really shows a photograph smaller than it is (`FaceIndex.downscaled`, the one condition — a picture the preview draws pixel for pixel has nothing more to say and is described from it as before), a detection whose shorter side on the preview is below `FaceDetection.REFINE_PIXELS` (112, SFace's own input) is looked up again in the file: `Originals.decodeLongSide` region-decodes the box grown by its own size on every side (`ImageReadParam.setSourceRegion` + `setSourceSubsampling`, never the whole raster — the memory rule of #68), YuNet runs on that region and the face overlapping the mapped-in box by more than `REFINE_IOU` (0.3) gives the box **and** the SFace embedding at full resolution, mapped back into the pixels of the preview (`FaceIndex.refine`, the one place the raw and the preview frame meet, `FaceDetection.Refiner` being the seam a test stubs); with that second look the pass keeps candidates from `MIN_FACE_PIXELS_REFINABLE` (24) px **only if** the original confirms them, while a look that finds nothing leaves a face of 40 px or more exactly as it was. The crop of `?type=face` is cut from the original the same way (`Originals.decodeShortSide` + `Originals.upright`, then down-scaled to `FaceIndex.CROP_MAX_PIXELS` = 512 on the shorter side) whenever the preview crop would be under `CROP_MIN_PIXELS` (256), falling back to the preview crop with one `LOG.info` where no reader can decode a region; `FaceCache` carries a per-face `refined` (absent = false; `true` means "described at the best resolution this build knows", so a face large enough on the preview carries it too), `FaceDetection.MODEL` is **not** bumped — an older `faces.json` reads unchanged — and the per-space walk re-describes exactly the photographs whose entry holds an unrefined face below `REFINE_PIXELS` (`FaceIndex.unrefined`), once, resumably, the album re-clustered afterwards and `Recognition` learning the better numbers the ordinary way. **The embeddings never leave the server**, and nothing is written beside the originals but the album's `.vacache` and the `.hashes.json` the index fills as an upload would. The packages need no new system library (`TestDebianPackageLibraries.testTheFaceIndexNeedsNothingNew` computes the closure of `libopencv_java.so` per packaged platform and finds nothing outside the base system; the artifact's GTK-linking `highgui` is never loaded), and cost the amd64 `.deb` +34.6 MiB for the models. Fixtures: `image-server/src/test/fixtures/faces/` holds four public-domain painted portraits, because YuNet detects a drawn synthetic face but SFace maps every such drawing to nearly the same point (0.83–0.91 cosine between clearly different drawings), so a synthetic fixture cannot carry the clustering assertion; a machine without a loadable OpenCV skips those tests loudly (`FacesTestCase.detectorAvailable`). **People and tags (#125)**: a space's people live in `<space>/.valbum/people.json` (`PeopleStore`, in the `faces` package beside `FaceIndex`, held by the `ImageServlet` — not by `AuthService`, which nulls its stores in `--auth off` — read at start-up, written atomically): `{version, people:[{id, name, cover:{path,face}, aliases:[id…], user, created, createdBy}]}`, the `id` sixteen random bytes base64url (22 chars), the name editable and unique ignoring case, `user` (#128) and `cover` (#126) stored and answered but never resolved here. **A merge writes an alias and rewrites no album**: `PeopleStore.resolve` answers a merged-away id with the survivor everywhere — in `?type=people`, in a rename, on every tag, and for a tag made *after* the merge that still names the old id. What somebody decided about a face is **stored** on the `ImagePart` as `repeated FaceTag tags {x,y,w,h, person, state}` with `FaceState{UNDECIDED, CONFIRMED, REJECTED, NOT_A_FACE}` (`UNDECIDED` first, so an untouched detection answers it; a stored tag is never `UNDECIDED`, a request carrying it is 400 `TAG_UNDECIDED`), the box copied at tagging time out of the `FaceInfo` the caller named and written in the raw raster like every other box, so it outlives the cache and the model; the stored statement is `tags` and the derived answer stays `faces`, and nothing clears `tags` before a write. `FaceTags` (beside `FaceIndex.derive`, one place, which also runs when the space's index is off but an album carries tags) merges them on every read: a detection overlapping a tag by more than `IOU_MATCH` (0.5) is answered its `person` (alias-resolved), `state` and `confirmed`, and **a tag no detection matches is answered as a `FaceInfo` of its own** behind the detected ones, without a cluster and without a crop (`?type=face` 404 — the app draws it from the picture and the box). `Faces.maySee` governs tags as it governs detections, from the other side — a tag is stored, so `Faces.withoutTags` takes it out for a share link, an anonymous caller and `?viewAs=public`, on the album and on a single image alike. Endpoints: `GET <data>/?type=people` → `PersonList` (`Person{id, name, cover, user, aliases}`; share link 403 `PEOPLE_REFUSED`, anonymous 401, `--auth off` everybody), `POST <data>/?action=create-person|rename-person|merge-persons` (`PersonCreate{name}`, `PersonRename{id,name}`, `PersonMerge{into,from}`; `edit`; 409 `personExists`, 400 blank/`MERGE_SELF`/`PERSON_UNREADABLE`, 404 `unknownPerson`), `POST <album>/?action=tag-faces` with `TagFaces{faces: FaceAssignment{image, face, person, state}}` → the album as the caller is answered it (`edit`; a **contribute-only member is refused 403 `TAGGING_REFUSED` even on their own photographs** (#123), a share link and a viewer likewise, an unknown image 404, an unknown face or person 400, a `NOT_A_FACE` with a person or a `CONFIRMED` without one 400, all-or-nothing), writing `index.json` through `storeSidecar` the way `?action=place` does and **replacing a tag on the same box** (the same IoU rule, in either direction) instead of adding a second; there is no way to remove a tag yet (#138). Tags ride on a move (#47) and into the trash (#131) with the `ImagePart`, and survive an album's and an inbox's PUT because a member is answered them and sends them back — a client that does not know the field would drop them, which is why tagging is an action and not a PUT. `image-server/src/test/fixtures/face-tags/index.json` pins the stored form. **Recognition (#127)**: once somebody is confirmed, the server recognises them elsewhere in the space. `Recognition` (`faces/Recognition.java`, one per space, held by `FaceIndex`) keeps **in memory only** the embeddings of every person's `CONFIRMED` faces, read back per album from the `FaceTag` in `index.json` and the matching detection in that album's `faces.json` (the same `FaceTags.IOU_MATCH` rule as the answer, never recomputed from pixels). It is filled on the **one** low-priority per-space walk of #124 (`FaceIndex.indexFolder`, so a start-up pass rebuilds it), refreshed by every listing (`derive` has both sidecars open anyway, so an album moved, written behind the server's back, or emptied by a delete is described afresh when it is listed), and updated **synchronously** when `?action=tag-faces` lands; `refresh-cache` (`FaceIndex.forget`) drops what that album contributed, and a changed model stamp drops everything. At derive time every detection carrying **no** decision is compared by cosine similarity to each person's nearest prototype (a dot product — everything is stored normalised) and the best person is suggested when the similarity reaches the threshold **and** leads the second-best person by the margin: `valbum.faceThreshold` (default SFace's `0.363`, the same number `Clustering.THRESHOLD` uses) and `valbum.faceMargin` (default `0.05`), both system properties and never UI settings. A person merged away resolves to the survivor when the matcher is built, so both sets of prototypes count for the survivor at once. The suggestion is `FaceInfo.person` with `confirmed: false` and `state: UNDECIDED`, derived at request time and **written nowhere**; it reaches only whom `Faces.maySee` lets it (an anonymous caller, a share bearer and `?viewAs=public` get none). **A face carrying any decision is never suggested anybody** — the wire holds one person and one state per face, a rejection already occupies both, so a rejection is "never a guess again" for that face and not merely "never that person"; `NOT_A_FACE` is never matched and a confirmed face is that person rather than a guess. `FaceIndex.answered`, the numbering a tagging request names a face by, deliberately carries no suggestion. `FaceState.UNDECIDED` in a `FaceAssignment` is no refusal any more but the way back out of a decision (#138): the tag on that box is removed by the same IoU rule that replaces one, the face is answered as the plain detection it was and #127 may suggest for it again (the prototypes are put anew, as after a confirmation); the person is ignored, an `UNDECIDED` for an untagged box is a no-op and a request that changes nothing writes no sidecar at all; the rights are the rights of `tag-faces`. **A person of the register may be a member of the space (#128)**: `POST <data>/?action=link-person` with a `PersonLink{id, user}` writes `Person.user` — the one place the link is stored — and answers the `Person`; `user: ""` unlinks. An administrator (`AuthService.mayAdminister`) links any person to any member; a member with `edit` links a person to themselves alone and may only unlink the person that carries their own name — anybody else 403 `LINK_REFUSED`, a share link 403 `PEOPLE_REFUSED`, an anonymous caller 401, an unknown person 404 `unknownPerson`, an unknown, nameless or pending member 404 `unknownUser`, a member another person already holds 409 `userAlreadyLinked(name)`, an unreadable body 400 `PERSON_UNREADABLE`. `merge-persons` of two linked people is refused 409 `MERGE_LINKED`; where one is linked the survivor keeps or takes the link. `?action=remove-user` unlinks the person and keeps them, tags and all, and says nothing about it in `UserList`. The link is answered from both ends: `?type=people` as `Person.user`, `?type=users` as `UserEntry.person` (the id) and `UserEntry.personName` (the name to show, so the users list needs no second fetch). It lies in `people.json` and survives a restart. In the app the link is made where the people are: the header menu of a named person offers **"This is me"** (key `persons-link-me`) to a signed-in caller with `edit` whom no person claims yet, **"Link to a member…"** (key `persons-link-member`) to an administrator — a chooser over `?type=users` showing the joined, named members no person holds (`linkableMembers`, `MemberChooser`) — and **"Unlink"** (key `persons-unlink`) to the administrator and to the linked member themselves; all three post `link-person` at once through `VAlbumClient.linkPerson` (`user: ""` unlinks), re-read the register (which is replaced, never merged), say a refusal in the server's own words, and the person's heading wears the badge "Member <name>" (key `persons-member-badge`), while the users section of the server screen says "Appears in photos as …" per member (key `user-person`, `UserEntry.personName`, read-only) and a `contribute` member sees none of it, having no header menu at all. **Named faces an older tool wrote are taken over** (#129): at the one moment a photograph is first analysed into an `ImagePart` (`ResourceCache.Loader.loadAlbum`, a file the sidecar does not list yet) in a space with `faces: on`, `FaceImport` reads the Metadata Working Group regions out of the file's XMP (`XmpFaces`, namespaces `mwg-rs`/`stArea`/`stDim` through metadata-extractor's `XmpDirectory` and Adobe's `XMPMeta`, wired as the `ImageData.Analysis` hook the servlet hands the cache) and turns every entry with `Type = Face` and a `Name` into a `FaceTag{state: CONFIRMED}` — a name somebody typed into Picasa or digiKam is a decision, not a guess. **The sidecar is the idempotence marker**: a part it already lists is never analysed again (#78's rule), so an import runs exactly once per photograph, the tags ride out on the album's first ordinary write like the date and the camera do, and a name taken back through `tag-faces UNDECIDED` stays gone. An MWG area is centre-based, normalised and written in the file's **stored raster before orientation**, which is exactly the frame a `FaceTag` is stored in (`Faces`), so the corner (`cx-w/2, cy-h/2`) is stored as it stands — nothing scaled, nothing turned — and the imported tag meets the detector's box of the same face by `FaceTags.IOU_MATCH` like any other pair. `AppliedToDimensions` must equal the file's own raw width and height (a `pixel` unit, dimensions the file omits taken as this file's own), else the **whole list is dropped** with one log line: a resized copy carries regions of a picture that is gone, and a box in the wrong frame names the wrong person. A `Pet`, `Focus` or `BarCode` region and an unnamed `Face` (that is a detection, and detections are #124's business) are ignored; the `Name` is looked up in the register ignoring case and outer blanks (`PeopleStore.named`/`byName`) and an unknown one **creates the person** with `createdBy: "import"` (`FaceImport.CREATED_BY` — nobody asked, so no `Caller.subject()` is claimed), who is then a person like any other, linkable to a member; the same name twice in one picture is one person and two tags. Photographs only (the hook is asked in the JPEG and PNG branches and never for a video), `faces: off` imports nothing, nothing is ever written into the original, and a malformed packet is one log line and no tag and never an exception out of `analyze`. `Recognition` learns the imported tags the ordinary way, when the album is described. The fixture `image-server/src/test/fixtures/xmp-faces/two-faces.jpg` is written by the test-only `Xmp` writer (the XMP counterpart of `Exif`).
- Spaces (#82, Phase 6): a server hosts one space or several. `Spaces.detect` decides the mode at start-up — **multi** as soon as one folder directly below the base folder carries `.valbum/space.json`, **single** otherwise; `--spaces auto|single|multi` overrides. `SpaceStore` reads `space.json` `{name, anonymous: none|public, mapUrl}` (missing/unknown values read closed; `mapUrl` is the #112 map template carrying `{lat}`/`{lon}`, `SpaceStore.DEFAULT_MAP_URL` = `https://www.google.com/maps?q={lat},{lon}` where the file names none or names a blank one, answered by `?type=auth` as `AuthInfo.mapUrl` — there is no provider enum, a provider *is* a template); a space is created on disk by hand, never by the server, and a new one is picked up by a restart. Each space has its **own** `AuthService` rooted at its own folder, so its `.valbum/users.json`, devices, invitations and albums are its own and a token of space A is simply unknown in space B (anonymous there). Anonymous access is a property of the space: `public` → the space runs like `--auth writes`, `none` → like `--auth all`; a server-wide `--auth all` is never loosened and `--auth off` stays off. Addressing in multi mode goes through `SpaceServlet` at the context root: `<context>/<space>/data/...` is that space's data root (the request is handed on with servlet path `/<space>/data`, so redirects, `Location.spell` and share URLs are spelled in the caller's coordinates), `<context>/<space>/` serves the app with `<base href>` rebased onto it (`ResourceServlet.setBaseSegments` + `WebRootResolver.singleSegmentBase`, the one-segment sibling of `/s/<token>/`), the app's own files are untouched; a session of a space lives **under** it — `<context>/<space>/s/<token>/` and `<context>/<space>/i/<token>/`, rebased by `WebRootResolver.spaceSessionBase` (the three-segment sibling of `/s/<token>/`), because the app opened there has to find a data root, and `ImageServlet.appUrl` derives that base from the request's own servlet path so a single-space server keeps spelling `<context>/s/<token>/` exactly as before; a session address at the context root of a multi-space server is refused with `SpaceServlet.sessionNeedsSpace`, and a first segment that is neither a space nor a file is answered `404` with an `ErrorInfo` (`SpaceServlet.noSuchSpace`). Single mode keeps every address it had. Bootstrap (#89, reversing #86's "a nameless admin only comes from an old library"): **every space has its admin from the moment it exists** — `AuthService`'s constructor writes a nameless `admin` (clearance `all`, `mayShare`, no devices) into a space without `users.json`; the seat is never empty, only unnamed, and in `--auth off` nothing is written. While that admin has **no device**, `Main.reportSpace` issues and prints one **seat code** per space (`AuthService.issueSeatCode(fixed)`: an ordinary `DeviceCodeStore` entry, `issuedBy = DeviceCodeStore.SERVER_ISSUER` (`"server"`, never a device id, so no issuer to die with), ten minutes, once): `This library: sign the administrator in with the code ABCD-EFGH (valid 10 minutes, once; restart the server for a new one).` A restart withdraws the earlier one and prints a new one — which is also the recovery when the last admin device is lost — and a space whose admin has a device prints nothing. `--admin-code <code>` fixes the code (validated by `DeviceCodeStore.checkCode`, alphabet and length, dashes ignored; the demo server uses `ABCD-EFGH`). Redeeming a code for a user **without a name** needs a non-blank `userName` — else 400 `NAME_REQUIRED` (generalising #86's `ADMIN_NAME_REQUIRED`) — and writes it on the user (`UserStore.checkUserName` + `isNameFree`, 409 `nameTaken`); for a named user the `userName` is a check as before (401 `DEVICE_CODE_OTHER_USER`). **`--pairing-secret` is retired**: given on the command line the server refuses to start (`Main.retiredSecret`, naming `--admin-code`), and a pairing carrying `PairRequest.secret` without a code answers 410 `SECRET_RETIRED` for one release; a pairing carrying nothing answers 400 `CODE_REQUIRED`. `AuthService.nameNamelessOwner(suggestion)` stays for a nameless admin **that has devices** (a library written before Phase 6, where nobody would ever be asked) — at start-up (`Main.reportSpace`, one printed line) or by `--migrate-to-spaces` (a report line) — the space's segment in multi mode, `owner` in single mode, with `-2`, `-3`… when a user or a top-level folder already holds it, and their devices and token hashes untouched, so every device keeps working. So a signed-in user always has a name and `Caller.subject()` never files their contribution as `anonymous`; every further user comes in through an invitation, which now carries `role`, `clearance` and `mayShare` (`Clearances` = `public|nonPrivate|all`, capped at the inviter's own), stored on the created user. `ImagePart`-style permission fields on the stored user (`clearance`, `mayShare` in `users.json`) and on `AuthInfo`/`UserEntry`/`Invitation` are written and answered here and enforced by #83. `?type=auth` names the space, the role, the clearance and the share flag. Migration is explicit: `--migrate-to-spaces` (like `--migrate-to-user`, refuses to start the server, rename-only, nothing deleted) turns an un-migrated library into the one space it already is (nothing moves) and a per-user library into a multi-space server — every user folder gets a `space.json` and a `users.json` holding that user as its admin with their devices, and `users.json`/`grants.json`/`groups.json`/`shares.json`/`invitations.json`/`device-codes.json`/`.valbum/guests/` plus every `.links.json` and `share-registry.json` in the tree are moved under `<base>/.valbum/retired/<timestamp>/` and named in a printed report saying what each held and what to do instead (`SpacesMigration`).
- Video renditions (#74): beside the poster frame, `VideoRenditions` (one per servlet, shut down in `ImageServlet.destroy()`) makes two sidecars per video in the album's `.vacache` — `video-<name>.mp4` (faststart, H.264 720 px short side, ≤30 fps, AAC) and `teaser-<name>.mp4` (~3 s from ~10 % in, silent, 240 px, faststart) — with the FFmpeg *program* of the bundled `org.bytedeco:ffmpeg` (`Loader.load(org.bytedeco.ffmpeg.ffmpeg.class)`), run as a child process with an argument list (never a command string), `-threads` at half the processors, one transcode at a time on a dedicated single-thread executor independent of the #69 preview permits. The encoder is chosen once by asking the program (`-encoders`): `libx264` if present, else `libopenh264` (what the LGPL build ships), else nothing is transcoded. `?type=video` / `?type=teaser` on an image path need `view` like the thumbnail and answer `video/mp4` over the existing range path; a rendition not yet made is never awaited — `202` + `ErrorInfo` `RENDITION_PENDING` + `Retry-After: 10` and the transcode is queued (deduplicated per rendition); a failed transcode is remembered for the process lifetime and answered `500` + `RENDITION_FAILED` with the FFmpeg tail in the log. Written to `.tmp` and moved atomically, invalidated like previews (`PreviewCache.LAST_UPDATE`); the original is never touched. The child process is started through `VideoRenditions.program(...)`, which sets `LD_LIBRARY_PATH` to the JavaCPP cache directory the program was extracted to, so the bundled libraries reach it on ARM too, where the program carries `DT_RUNPATH` (which the loader does not apply to indirect dependencies) rather than `DT_RPATH` (#87); what no platform jar ships — `libxcb1`, `libxcb-shm0`, `libxcb-shape0`, `libxcb-xfixes0` and ALSA — the Debian package recommends (`image-server/src/deb/control/control`, checked by `TestDebianPackageLibraries` against the ELF `DT_NEEDED` entries of the bundled artifacts).
- Preview generation (#69): `PreviewCache.createPreview` throttles only the *generation* of a preview — `--preview-threads <n>` / the system property `valbum.previewThreads` / `availableProcessors()` permits of a `Semaphore`, deliberately not a limit derived from free memory (the reasoning is in the Javadoc of `PreviewCache.permits`); serving a preview that is already cached takes no permit. A `ConcurrentHashMap` of in-flight futures keyed by the preview's absolute path makes concurrent requests for the same uncached file wait for the one generation and share its outcome (the same `PreviewException` for every waiter, the entry dropped so a later request may retry); the preview is written to `preview-<name>.tmp` in `.vacache` and moved into place with `ATOMIC_MOVE` (falling back to `REPLACE_EXISTING`), so nothing ever serves a half-written file. Refreshing (#98): `POST <folder>/?action=refresh-cache` (admin only, `AuthService.mayAdminister`, else 403 `CACHE_REFRESH_REFUSED`) deletes in that one folder's `.vacache` exactly what the server generated — `CacheRefresh.isGenerated`: `preview-*`, `video-*.mp4`, `teaser-*.mp4` and those under their `.tmp` name — never recursive, never a sidecar or an original, forgets the folder's failed-transcode memory (`VideoRenditions.forget`) and answers `CacheRefreshed{removed}`.
- Static web content: the server serves `META-INF/resources` of the class path (the Flutter web build, icons included, if `valbum_ui/build/web` existed at `package` time) at the context root; `--webroot <dir>` serves a directory from disk with precedence over the class path. Unknown paths without a file extension fall back to `index.html` (client-side deep links).
- Test fixtures live at `image-server/src/test/fixtures/test-album`.
- Java lint/format (Spotless, lightweight — tidies imports/whitespace, preserves tab indentation; **not** bound to a build phase): `mvn spotless:check` / `mvn spotless:apply`.

### Flutter (`valbum_ui/`)

- Use the `flutter` CLI: `flutter pub get`, `flutter analyze`, `flutter test`, `flutter run -d chrome`. The Flutter SDK lives in `~/flutter`, the Android SDK in `~/Android/Sdk` (both on PATH via `~/.bashrc`).
- An album of kind `INBOX` is shown by **its own screen** (#136, `valbum_ui/lib/inbox_view.dart`), which the router picks in `VAlbumState.visitAlbumInfo` at the album route only — the viewer and the group views below it are the ones an album uses, an inbox simply never has a group. It is **always in the selection mode and buffers nothing**: no `AlbumEditSession` is ever opened, so there is no Save, no Cancel, no dirty guard (#99 — `leaveAlbum` lets every way out pass, a standing selection included), no reorder, no drag handle, no heading of its own, no rating, no index picture, no groups and no "View as". Its **headings are derived** (`inboxDays`, never stored, like `effectiveDate`; a group a stale cache still holds is read as its members): the server answers the parts flat and sorted by date (`Inboxes.flatten`), the screen draws one day heading per day (`inbox-day-<yyyy-MM-dd>`, `DateFormat.yMMMEd()`) with a month line where the month changes (`inbox-month-<yyyy-MM>`, `DateFormat.yMMMM()`, a year boundary being just another month change) and one `Undated` section at the end; **tapping a heading selects everything under it and a second tap takes it back**, which is how a day or a whole month is moved into an album in one gesture. Every action **writes at once** through `InboxContentState.writeNow(edit, undo)` — refuse offline (`refuseWhileOffline`), apply, `saveAlbum`, and on a refusal take the change back and say the server's own reason: rotation, privacy (the author's decision on #131 — the level is set before a photograph is sorted and travels with it) and "Correct time" (#77/#102, `use-name-date` included, which refiles by date so the days regroup by themselves); the sidecar sent is the inbox as the server answered it with the edited part substituted, and the server restores its own arrangement (`Inboxes.restoreArrangement`). The two ways out are `move-to` (`moveWithPicker`, the picker's "Create new album…" of #114 prefilled with `newAlbumDay` of the selection) and `delete-selection` (`?action=delete` naming the photographs, confirmed with `inboxDeleteExplanation` — "The photographs are moved to the trash folder of the space. Nothing is deleted from disk." — and the server's outcome read out verbatim). A tile's tools are its selection box, the three turns, `inbox-open` (the viewer — a tap *selects*, so the viewer is a tool), `inbox-adjust-time`, `inbox-properties` and `privacy-control`; the sidecar writes are offered with `edit` alone, the move and the delete also to a `contribute` member sorting **their own** contributions (#53/#135, the server refusing the rest with `CONTRIBUTION_REFUSED`), who therefore sees no turn, no privacy control and no properties. An image of an inbox carries **no description**: `ImagePropertiesDialog(editable: false)` (key `image-properties-read-only`) shows the details block and the attribution and nothing to type in, wherever it is opened — the tile tool and the viewer's long press alike. In the listing an inbox entry stands **first** (`sortedFolders` ranks `FolderKind.inbox` before everything, mirroring `ResourceCache.BY_DATE`), carries an inbox icon instead of the folder icon, shows no `folder-date` (`folderHasDate` is an album's) and is never offered `Share link…` (the server answers `INBOX_NOT_SHARED`); the tile of an inbox says how much is waiting (#137): `FolderInfo.imageCount` is the number of `ImagePart`s the inbox's own sidecar lists (the members of an `ImageGroup` counted one by one), filled in `ResourceCache.Loader.loadFolderInfo` for `FolderKind.INBOX` alone out of the sidecar it reads anyway (no image is ever opened, an album or a folder of folders answers `0`, a delete of #131 is reflected on the next listing), derived like `kind` so `AlbumDate.clearDerived` can never store it, carried through every copy of a `FolderInfo` (`PrivacyFilter`, `Inboxes.uncovered`/`entry`, `withRights` by reference), and **the whole count for everybody** — a `contribute` member shown only their own contributions inside (#135) is answered what the folder holds, because the sidecar knows no contributors and a count of one's own would cost a second file per entry of every listing; the listing tile of an inbox says "<n> photographs" (key `inbox-count`, an ICU plural) in the line an album's date stands in, nothing at all for a count of `0` — which is what an empty inbox and a server built before the field both answer — and never on an album's tile whatever `imageCount` it carries; the `folderLine` of the move picker is unchanged. The kind is edited where the album's properties are: `AlbumPropertiesDialog` carries the switch `album-kind` (`AlbumProperties.kind`, offered with `mayChangeKind`), whose label names what the tap does, and an inbox's dialog shows neither the date row nor the album picture; the switch stands **where the "Albumbild" label stood**, because the dialog must not grow — a scrollable one takes the crop editor's pan gesture away from it. `CreateAlbumDialog` offers the same choice (`create-kind-inbox`): ticking it hides the date field and `albumFolderName(null, title)` names the folder by its title alone. The camera-roll sync creates `Inbox` **as an inbox** (`_ensureInbox`); an album of that name that is already there is adopted with the kind it has. Offline the inbox is read from the cache and every action is refused with the usual reason; inside a share link the server answers 404 and the app shows the confined-link page it always did.
- A folder's own picture is chosen on a tile (#110): the listing tile's long-press menu offers an editor outside a share link **"Use as folder picture"** (key `use-as-folder-picture`) on every entry, album and folder alike, and **"Use no folder picture"** (key `clear-folder-picture`) where `ListingInfo.index` already names one. Both write *this* folder's own listing through the ordinary sidecar PUT — what was read with the one field changed, the server dropping the derived `folders` — and then forget the **parent** path and reload (`ListingView.setFolderPicture`): the tile that changed is this folder's own, one level up, where the server now answers `FolderInfo.indexPicture` with the path from the chosen child down to the photograph. `editFolder` carries `index` along untouched, so writing a title never takes the picture away. The tile itself needed nothing: `buildFolderPicture` builds `<listing>/<name>/<image>`, which with a path in `image` is the ordinary address `<data>/F/A/a.jpg?type=tn`, and `indexPictureTile` turns it by the crop's own frame (#115) exactly as an album's.
- The face editor (#126) is the app half of #124/#125: the album menu offers **"Persons in this album"** (key `persons`) where `?type=auth` says `faces`, the album carries a face or `facesPending`, and the caller is not in a share session, and it opens `<album>/persons` — a page level of #93 (`PersonsRoute`, the reserved segment `persons`, `levelsOf` putting it on its album, the way back the app bar's `leading`, a deep link opening both pages with one fetch). `lib/persons_view.dart` groups every face of the album by what the server said: one group per **person** (`?type=people`, loaded once, `client.people()`), the **suggestions** of #127 under that person as a group of their own with "Is this …?" and a *Confirm* button, one per unknown **cluster** ("Who is this?", numbered where there are several), and a collapsed **"Not a face"** last. A face tile is `?type=face&face=<index>` through `FaceImage` (the `ThumbnailImage` of a crop, same token, same offline cache); a **tag without a detection** — appended behind the detections, no cluster, no crop — is drawn from the photograph's own thumbnail clipped to its box and turned by `orientedBox`, and no crop is requested for it (the file's EXIF rotation is not on the wire, so the clip is measured in the file's upright frame). Tapping toggles a face, the count stands in the app bar, a long press shows the whole photograph in a dialog (never the viewer route, which is a level of the *album* and would unmount the editor), and the #94 mechanics carry it: the tile on a sideways pull, a `drag-handle` in any direction, `DraggedFaces` carrying the whole selection, the group headings and **"New group"** as drop targets with the same highlight. **Person operations are space-level and post at once** (`create-person`, `rename-person` — "Renames this person everywhere." —, `merge-persons`, which rewrites the editor's own maps because the server resolves the alias anyway); **assignments are buffered** like an album edit — `placement` against `stored`, keyed `<image>#<index>` — and Save posts one `tag-faces` with the difference alone: a person group → `CONFIRMED`, out of one → `REJECTED` *with the person it stood under*, "Not a face" → `NOT_A_FACE`, a move between two of the server's guesses → nothing, a face that did not change (carried away and back included) → nothing; then the album is forgotten and fetched anew. Save/Cancel/`Escape` and the router's guard are the album's (`VAlbumRouterDelegate.registerPersonsGuard`, asked before `leaveAlbum` for every way out, Save / Discard / Stay), a 403 shows the server's own sentence and **keeps the buffer**. Only `edit` gets the controls: a `contribute` or `view` caller sees the groups and one line "Only editors may name faces" (key `persons-read-only`). While `facesPending` a banner says so (key `persons-pending`) and the album is asked again every ten seconds — never while something is unsaved — and an album with no face at all says so instead of showing an empty page (key `persons-empty`). A decision is taken back in the editor too (#138): a face carrying one is dropped on **"Forget"** beside "New group" (key `persons-forget-target`) or forgotten through the selection menu of the app bar (key `persons-forget`, offered only while something selected carries a decision), the buffer marks its placement `forget:<the group it falls back to>` so that a rejected face taken back is told apart from one merely standing in its cluster, the face falls back into that cluster ("Who is this?" where it has none) and Save posts `{person: "", state: UNDECIDED}` for it — never for a face the server decided nothing about — while a person group the buffer emptied stays on the screen so that a forgotten face can be dragged back onto it, which then posts nothing. **Correcting faces is the everyday act, so the editor is worked like a file manager (#139)**: the pointer kind of the click decides (`GestureDetector.onTapUp`, `TapUpDetails.kind`) — a finger keeps **toggling** (`toggle`, it has no modifier keys), a mouse or trackpad gets the regular semantics of `faceSelectionAfterClick` (`persons_view.dart`, pure over the ordered keys, the anchor, the modifiers and the current selection, so it is checked without a screen): a plain click **replaces** the selection, ctrl (and meta) **toggles**, shift takes the **range** from the anchor in `displayOrder` — every face of the page as drawn, groups in their order, "Not a face" only while it is open, so a range spans group boundaries — and the range takes the **anchor's own state**, so a range is deselected from an anchor that was just clicked off; the anchor is the face of the last plain or ctrl click, a shift-click does not move it, and a shift-click without a living anchor is a plain click (`_read` clears it). A **second click of a mouse on the same face shows the photograph** — counted in `handleTap` against `kDoubleTapTimeout` rather than through `onDoubleTap`, which would hold the gesture arena open and make *every* selecting click of this page wait a third of a second; **only a plain click counts, on either side** — a ctrl- or shift-click is neither half of "show me this photograph" and forgets what stood before it (`_forgetClick`, called as well wherever something opens over the page: the context menu, the photograph) — and a finger keeps its long press, where a second tap means "deselect". The photograph opens **large** (`markedPhoto`: up to 90 % of the viewport, the rendition at the file's own aspect ratio with the face's box drawn on it, key `persons-photo-box`, the pair turned together by `orientedBox` because the box is normalised in the **raw raster**, exactly as `FaceTile.clipped` measures it, the file name under it, key `persons-photo-name`) — still a dialog and never the viewer route. The page **scrolls while a face is carried** near an edge: `DragEdgeScroller` moved out of `album_view.dart` into `lib/drag_scroll.dart` (#42, unchanged) and is driven here exactly as the album drives it — the `ListView`'s own `ScrollController`, a `Listener` around the body tracking the pointer, `begin`/`update`/`end`/`dispose`, and the synthesized zero-length move of `_followScrolledContent` so a drop target scrolling under a resting pointer still takes the drop. The selection is acted on **without dragging**: the app bar's `persons-selection-menu` is offered whenever `mayEdit && selection.isNotEmpty` and holds **"Name person…"** (key `persons-name`, the `PersonChooser` of a group heading, creation included, placing every selected face into that person's group → `CONFIRMED`), **"Defer (new group)"** (key `persons-defer`, what dropping on "New group" does) and **"Forget"** (key `persons-forget`), and the **same three** open as a context menu on a tile by secondary click behind **"Show the photo"** (keys `persons-context-show|name|defer|forget`, positioned at the pointer through `showMenu`), acting on the whole selection when the right-clicked face is part of it and otherwise on that face alone, which first *becomes* the selection. "Forget" now takes back **both** kinds of statement (`canForget`): a stored decision is marked `forget:` as before, and a placement that is only in the buffer is simply dropped, so the face stands where the server put it and Save writes nothing for it; it is left out where nothing selected has anything to take back. A suggestion group offers **"Someone else…"** beside *Confirm* (key `persons-someone-else-<person>`, `nameGroup`): the guess is simply not confirmed and the chosen person is, which is one `CONFIRMED` per face and no `REJECTED` — choosing the suggested person is the Confirm. Not yet: a keyboard way to the range (the click is the only one).
- Localization (#108): every user-facing string of the app lives in `valbum_ui/lib/l10n/app_en.arb` and is read as `AppLocalizations.of(context)!.<key>` (`lib/l10n/app_localizations.dart`); the **platform locale** decides and there is no switch in the app, both `MaterialApp`s carrying `AppLocalizations.localizationsDelegates`/`.supportedLocales`, `onGenerateTitle` for their own name and `resolveAppLocale` (`lib/locales.dart`) as `localeListResolutionCallback`, which matches the device's preferences in their order, exactly and then by language, and answers **English** for a locale the app does not carry (the generated list is alphabetical, so the default would have been German). **`app_en.arb` is the only file edited by hand** — `app_de.arb` (and every further language) is written by `gradle -p valbum_ui translateArb` (the Gradle plugin `de.haumacher.auto-translate-arb` in `valbum_ui/build.gradle`, DeepL, key `deepl.apiKey` in `~/.gradle/gradle.properties`, incremental, placeholders and ICU plurals kept) and every `app_localizations*.dart` by `flutter gen-l10n`; all of them **are committed**, the same doctrine as `lib/resource.dart`, so a clone and CI need no DeepL key. Adding a string is: key + text + `description` into `app_en.arb` (a name is a placeholder, a count an ICU plural, and a placeholder is never put at the start of a sentence — DeepL capitalises it and changes the generated signature), then `translateArb`, then `gen-l10n`. A helper that composes words outside a widget takes an `AppLocalizations` parameter rather than keeping an English fallback (`serverUrlError`, `lastDeviceWarning`, `CallerPermission.sentence`/`phrase`/`roleWord`/`clearanceWord`, `userDisplayName`, `defaultDeviceName`, …); a refusal keeps its visible message, localized, and an unreadable server address is refused with `serverUrlInvalid`, never with the parser's sentence. Server messages stay as the server spells them (`VAlbumException.message`), and `nameRequiredMessage` stays a plain constant because it is *matched*, not shown. Tests pump through `test/util/l10n.dart` — `localizedApp(widget, locale: …)` and `l10nOf(locale)`/`testL10n` — and keep asserting the English text through the `en` locale; `test/l10n_german_test.dart` is the German smoke test of the converted screen. `test/l10n_guard_test.dart` holds the line: every language carries the keys **and the placeholders** of `app_en.arb`, no file in its `convertedFiles` list holds a user-facing literal any more (a technical one is named in `allowedLiterals` with its reason), and no converted file throws a sentence a reader could see (`allowedThrownLiterals`). **A slice that converts a screen adds its files to `convertedFiles` and its German assertions to the smoke test.** Slices 1 and 2 converted the server settings screen and what it is made of, the share link and the share session, the camera-roll section, the alternatives view, the video player, the photo picker, the upload dialog and the code scanner; **slice 3 converted everything that was left — the album page and its edit mode (`album_view.dart`, `album_edit.dart`), the listing and its dialogs (`listing_view.dart`), the move and the delete (`move_view.dart`), the inbox screen (`inbox_view.dart`), the viewer (`image_view.dart`), the image properties (`image_properties.dart`), the attribution (`attribution.dart`), the rights (`rights.dart`), the rest of `app.dart` and `invitation.dart`'s `invitationNoticeText`, the offline banner and its refusal (`offline.dart`), the transport (`client.dart`, `cache_refresh.dart`) and the engines (`camera_roll.dart`, `background.dart`, `connectivity.dart`, `photo_library*.dart`, `platform_io/web.dart`)** — so the whole app speaks the language of the device and `convertedFiles` names **every** hand-written file under `lib/` (a guard test holds the list against the directory, with `resource.dart` and the generated `l10n/` the only ones out). An engine that has no `BuildContext` answers **data, not words**: `lib/notices.dart` holds the sealed `AppNotice` (`GuestHasNoSpace`, `NoWifiMobile`, `PhotoAccessDenied`, `ServerUnreachable(problem)`, …) and the one function `noticeText(notice, l10n)` that turns it into a sentence where it is shown — `CameraRollStatus.notice`, `PhotoLibrary.accessProblem`, `BackgroundScheduler.unavailableReason`, `NetworkKind.refusal`, `CameraRollSync.setEnabled`, and `PhotoLibraryException` carrying a library's own reason out of `photo_library_manager.dart`; `CameraRollStatus.message` is what the **server** said, verbatim and never translated, `inboxNotice` became `inboxGoneUsing` (the album name, not the sentence), and the lines themselves are composed by the view (`cameraRollLine`, `backgroundRunLine` in `camera_roll_view.dart`). The transport and a background isolate have no widget tree and still compose sentences a reader sees, so they read `platformMessages` (`locales.dart`): `lookupAppLocalizations(resolveAppLocale(appPlatformDispatcher.locales, …))`, where `appPlatformDispatcher` is the **binding's** dispatcher wherever a binding exists (every screen and every test — `flutter_test` wraps it, so `tester.platformDispatcher.localesTestValue` reaches the transport too) and the engine's `PlatformDispatcher.instance` only in the background isolate that never initialised one — no layer keeps an English fallback. 583 keys, and what stays English is named in the guard's `allowedLiterals` with its reason: the server's own sentences, the `diagnostics.dart` bug report and every `kDebugMode` log, the `offline_file.dart` cache log, the `video_view.dart` failure keywords, every `toString()`, assertion, regular expression and format pattern — and `defaultInboxName`, which is a folder name on disk and stays `Inbox` in every language; `diagnostics.dart` stays English, being a bug-report log. Dates shown to a reader go through `DateFormat.yMMMd()` and friends, which follow the locale; `folderDateFormat` (`yyyy-MM-dd`) is the folder-name convention and stays.
- Server URL: on the web derived from the page origin (`lib/urls.dart`, `deriveDataUrl`); on other platforms the default `http://localhost:9090/valbum/data` until the settings screen (ROADMAP Phase 2). All HTTP goes through `lib/client.dart` (`VAlbumClient`), injected via `VAlbumScope` — tests pass a `MockClient`. Sessions (#51, #52): an app base `<context>/s/<token>/` or `<context>/i/<token>/` is a share link or an invitation (`urls.dart` `sessionUrl`/`SessionUrl`, one mechanism, two kinds); the token is the bearer for one `?type=auth`, whose answer opens the album (`share_session.dart`), the welcome-and-join screen (`invitation.dart`), or — for a token the server knows as neither — the ordinary start. The device's stored server and token are never read in a session and a session token is never stored; joining stores the plain server URL and the device token like a sign-in, then `leaveForUrl` (`platform_*.dart`) replaces the location with `<context>/`. A dead invitation address is no dead end (#88): the server redirects the page to the ordinary app base with `?invitation=<reason>`, which an ordinary start reads once from the location, removes again with `rewritePageUrl` (`platform_*.dart`, `history.replaceState`) and shows as a dismissible `MaterialBanner` over whatever it shows anyway (`invitationNoticeText` in `invitation.dart`, injectable through `VAlbumApp.location`/`rewriteLocation`); an invitation that dies between the page load and the probe keeps its reason and offers `Continue to the start page`. `serverLocationOf` lets the settings' server field accept a pasted invitation URL (and refuses a share link there). The caller's role comes from one `?type=auth` per client (`caller.dart`, `CallerScope`) and hides at a guest's own root what the server refuses by role. Camera-roll sync (#30) watches **the device albums the user ticked** (#117, `camera_roll_view.dart`, keys `cameraRoll.source.<id>`), never the whole photo library: `CameraRollConfig.sources` holds them, a store written before that field watches the camera album alone (`defaultSources`, `PhotoManagerLibrary.isCameraPath` — Android's `DCIM/Camera`, iOS's `smartAlbumUserLibrary`, the *Recents* pseudo-album never a source of its own) and the whole library only where the device names no album at all (`CameraRollConfig.wholeLibrary`); every watched album carries its own watermark (`SourceMark`, `CameraRollConfig.markOf`), so an album ticked later is fetched from the beginning while the others keep their place, and an item lying in two ticked albums is uploaded once. While the server is still building its hash index (#118) a run **defers**: `uploadNew(..., waitForIndex: true)` reads `UploadCheckResult.indexed` from the check it makes anyway and, where `done < total`, transfers nothing and answers `UploadSummary.deferred`; the sync goes to `CameraRollPhase.waiting` with `indexingNotice(done, total)` — "The library is still being indexed (… of … folders); photos already in an unindexed album may be uploaded again." — is retried by the ordinary timer and offers *Sync anyway* in the section (key `cameraRoll.syncAnyway`, `CameraRollConfig.syncWhileIndexing`, default off and persisted). A stored inbox the server answers **404** for — renamed by a #130 properties write — makes the run fall back once (#132): `_ensureInbox(client, replace: true)` is the create-or-adopt rule of a first run, the new path is stored with the watermarks untouched, the batch is retried there, and `inboxGoneNotice` ("The chosen inbox is gone; using 'Inbox'.", `CameraRollStatus.inboxNotice`) stands on the line of the run that worked until the next success or `chooseInbox`; any other refusal is told as before, a refused fallback keeps the stored choice, a second 404 fails ordinarily, and a run with nothing new still asks nothing. A photo the server already holds **elsewhere** is named by its path: `UploadSummary.presentIn` and `CameraRollStatus.lastPresentIn` carry it into the summary and the idle line. The album menu offers whoever may edit **"Find duplicates…"** (key `find-duplicates`), which confirms ("Nothing is deleted"), posts `?action=find-duplicates` through `VAlbumClient.findDuplicates` and says how many photos were set aside. Chrome (#100): the way back is the app bar's `leading` on every page (listing, album edit mode, viewer) and the three-dots menu is the last control at the right; "View as" is no button any more but entries of `albumMenu` (a `menuLabel` naming the view on the screen, the three choices under it, keys `view-as-<name>`). Since #121 **"Album properties"** (key `album-properties`) and the move (key `move-to`) are entries of that menu too, offered in the view mode as well as in the edit mode to a caller with `edit` and never in a share session: the properties edit the buffer inside an edit session and write the album by themselves outside one (refused offline with `refuseWhileOffline`, the change taken back and the server's reason said on a refusal), and the move reads `Move <n> images to…` while the edit mode holds a selection and **`Move album to…`** otherwise — the album's own folder moved out of the listing above it (an `EntrySubject`, never at the space root), the app ascending to that listing afterwards. Save and Cancel are the only icons the edit toolbar keeps. One `ImagePropertiesDialog` (`lib/image_properties.dart`, #122) is what an image says wherever it is asked: the details block (keys `property-file`, `property-time`, `property-camera`, each line only where there is something to say, plus `property-location` (#112) where `ImagePart.location` is set: the coordinates as `48.123456, 8.654321` — six decimals, a dot whatever the locale, `mapCoordinate`/`mapLocationText` — beside a map button (`property-location-map`) opening `mapUrlFor(template, location)` through `url_launcher`, the template being `CallerInfo.mapUrl` from `?type=auth` and `defaultMapUrl` where an older server named none), the attribution note and the description field — opened by the tile tool and by the viewer's long press alike, the viewer keeping its "write now vs. into the edit buffer" logic around the answer; its words are English ("Image properties", "File", "Taken", "Camera"). The album menu also offers an administrator **"Refresh previews"** (#98, key `refresh-previews`), which confirms, posts `?action=refresh-cache` through the `CacheRefreshClient` extension in `lib/cache_refresh.dart`, clears Flutter's whole `ImageCache` (`forgetDecodedThumbnails` — the resized tile keys cannot be enumerated) and reloads. The move dialog offers only targets of the kind the move can go to (#113, `move_view.dart`): with images the confirm button is enabled on an album alone and a folder of folders says “Images live in albums — open one.” (`picker-needs-album`), with an album or folder the other way round (`picker-leaf`); every entry is one line, `FolderInfo.effectiveDate` as `yyyy-MM-dd` and the title, composed by `folderLine`, never the folder name beside it. For images the picker also offers **“Create new album…”** (#114, key `picker-create-album`) where the caller may `contribute` to the folder shown: it opens the listing's `CreateAlbumDialog` with the earliest date of the selection (`newAlbumDay`), and `moveWithPicker` creates the album and then moves into the path the `CreateResult` answered — a refused creation moves nothing, a refused move says that the album is there and empty. `lib/name_date.dart` is the Dart twin of the server's `ImageData.nameDate` (#102), pinned by the shared table `image-server/src/test/fixtures/name-dates.json`; the "Correct time" dialog offers "Use the time in the file name" (key `use-name-date`) where a name says a time the image does not carry, each selected image getting its own. In the edit mode every tile carries a drag handle (#94, key `drag-handle`, `ReorderablePart.handle`) that lifts it in any direction, sharing `DraggedParts`, the drop targets and the edge scrolling with the tile's own sideways pull. An album edit is ended by Cancel (the `×` of the edit mode's app bar, and `Escape`), which asks "Discard the changes to this album?" while the buffer is dirty and then re-fetches the album, and every way out of an album — the way up, the home button, a listing tile above, the system's or the browser's back button, a deep link — passes one guard in the router (`VAlbumRouterDelegate.leaveAlbum`, the mounted album registers its `AlbumContentState.confirmLeave` per path), which drops a clean edit session silently and asks "Save the changes to this album?" (Save / Discard / Stay) for a dirty one (#99). Navigation keeps one page per route level (#93): `VAlbumRouterDelegate.levelsOf` turns the route into the page stack (album → image / alternatives → member), so ascending pops and finds the album still mounted, with its state, its scroll offset and its thumbnails held live (each tile keeps its own listener on the image stream, which a covered page's `TickerMode` would otherwise drop), and a tile decodes its thumbnail at the height it is drawn at (`resizedThumbnail`, `ResizeImage` at display height × device pixel ratio, never upscaled). Because of that, **every write that changes a folder other than the one shown forgets what the router holds for it** (#134, `VAlbumRouterDelegate.forgetTree(path)`: the path and everything below it, by segments, the root meaning everything): `moveWithPicker` forgets the picked target's tree (a placement rule may file the album into a year folder inside it) on a move and on the created-but-refused branch, album creation and `?action=place` the folder's tree, and a #130 rename the old path's tree and its parent; a delete changes only the parent, which is reloaded anyway. The viewer shows what the caller may have (#95): the original with `download`, the preview rendition (`?type=tn`) without — so a `view`-only visitor is never told about a right they were not offered, and only a genuine failure speaks, once, as a message over the thumbnail; it prefetches the pictures of its two neighbours with the same provider it would display them with and keeps that thumbnail beneath the picture, so paging is thumbnail → sharp and never blank → sharp (#101, `viewerPicture` in `image_view.dart`); that underlay asks for the thumbnail with the key the *tile* decoded it under (#120, `decodedThumbnailHeight` in `thumbnails.dart`, filled by `resizedThumbnail` — a `ResizeImage` resolves its inner provider outside the `ImageCache`, so the raw provider was a second download of the picture the album already had; only a deep link straight into the viewer, where no tile has drawn, still fetches it once). A video is turned in the viewer by its stored `ImagePart.orientation` exactly as its tile is (#116): `orientedBox` in `oriented_thumbnail.dart` — the one wrapper of #106, mirror first, then quarter turns — wraps `VideoView`'s poster **and** its playing surface together (never one without the other, and never the controls or the refusals), and the slot is fitted to the oriented dimensions (`Orientations.width/height`); `IDENTITY` wraps nothing. The index picture of a listing tile is turned by that same wrapper (#115): `ThumbnailInfo.orientation` is the frame the crop was made in — stored beside the crop and never derived, so an old sidecar without it reads `IDENTITY`, which is the frame such a crop was made in — `indexPictureTile` turns the rendition **before** applying `thumbnailTransform` (the square tile costs the turn nothing) and the crop editor draws through the same function, `indexPictureOf` measures the crop on the *oriented* size and writes the image's orientation onto it, `syncIndexPictureOrientation` carries the crop into the new frame when the album picture itself is rotated in the edit mode (the crop values kept, a quarter turn may shift what the square shows), and the server stores and answers the field untouched — `FolderInfo.indexPicture` is a copy of the album's, and the covers the server computes itself (`PrivacyFilter.thumbnail`, the sidecar-less fallback of `ResourceCache.loadFolderInfo`, `MoveService`'s re-pick) are framed in the frame of their image and say so. A share session is the album as its owner sees it (#97): immersive, the album's own heading once, no app bar and no link label over it (the label names the link where links are managed), and it shows no attribution at all — "Added by …" is bookkeeping among the members of a space (#96). On the web the server screen builds no address section and no camera-roll section — a browser talks only to the server it was loaded from and has no camera roll — and names that server in one line instead (`ServerSettingsScreen.isWeb`, defaulting to `kIsWeb` and injected by widget tests, #90). The caller's role comes from one `?type=auth` per client (`caller.dart`, `CallerScope`) and hides at a guest's own root what the server refuses by role. Chrome (#100): the way back is the app bar's `leading` on every page (listing, album edit mode, viewer) and the three-dots menu is the last control at the right; "View as" is no button any more but entries of `albumMenu` (a `menuLabel` naming the view on the screen, the three choices under it, keys `view-as-<name>`). Since #121 **"Album properties"** (key `album-properties`) and the move (key `move-to`) are entries of that menu too, offered in the view mode as well as in the edit mode to a caller with `edit` and never in a share session: the properties edit the buffer inside an edit session and write the album by themselves outside one (refused offline with `refuseWhileOffline`, the change taken back and the server's reason said on a refusal), and the move reads `Move <n> images to…` while the edit mode holds a selection and **`Move album to…`** otherwise — the album's own folder moved out of the listing above it (an `EntrySubject`, never at the space root), the app ascending to that listing afterwards. Save and Cancel are the only icons the edit toolbar keeps. One `ImagePropertiesDialog` (`lib/image_properties.dart`, #122) is what an image says wherever it is asked: the details block (keys `property-file`, `property-time`, `property-camera`, each line only where there is something to say, plus `property-location` (#112) where `ImagePart.location` is set: the coordinates as `48.123456, 8.654321` — six decimals, a dot whatever the locale, `mapCoordinate`/`mapLocationText` — beside a map button (`property-location-map`) opening `mapUrlFor(template, location)` through `url_launcher`, the template being `CallerInfo.mapUrl` from `?type=auth` and `defaultMapUrl` where an older server named none), the attribution note and the description field — opened by the tile tool and by the viewer's long press alike, the viewer keeping its "write now vs. into the edit buffer" logic around the answer; its words are English ("Image properties", "File", "Taken", "Camera"). The album menu also offers an administrator **"Refresh previews"** (#98, key `refresh-previews`), which confirms, posts `?action=refresh-cache` through the `CacheRefreshClient` extension in `lib/cache_refresh.dart`, clears Flutter's whole `ImageCache` (`forgetDecodedThumbnails` — the resized tile keys cannot be enumerated) and reloads. `lib/name_date.dart` is the Dart twin of the server's `ImageData.nameDate` (#102), pinned by the shared table `image-server/src/test/fixtures/name-dates.json`; the "Correct time" dialog offers "Use the time in the file name" (key `use-name-date`) where a name says a time the image does not carry, each selected image getting its own. In the edit mode every tile carries a drag handle (#94, key `drag-handle`, `ReorderablePart.handle`) that lifts it in any direction, sharing `DraggedParts`, the drop targets and the edge scrolling with the tile's own sideways pull. Signing in is **one widget** (`sign_in_form.dart`, `SignInForm`: the code field with the scanner, the device name, the name field on `NAME_REQUIRED`, the sign-in button; `saveServer` stores the server together with the token, `initialCode` takes what a QR code carried) used in three places (#91): the sign-in section of the server screen, the **refusal page** of a server that shows nothing to anonymous callers (`app.dart` `buildSignInRequired` — a browser already knows its server, so the code field is there and the album follows, the server screen staying reachable for devices/people/cache/diagnostics), and the app's **first screen off the web** (`first_screen.dart`, "Where is your album?": one field taking a server address or an invitation link, the QR scanner beside it, then the same sign-in form; a share link is refused there, an invitation opens the welcome screen as clicking it in a browser does; on the web this screen never appears, the origin being the server). Where a code is shown (`manage_view.dart` `DeviceCodeDialog`, also `backup: true` for the backup code of #92) it is offered three ways — the characters, the QR code, and the **same payload as a copyable link** (`valbum-device://pair?server=…&code=…`). The devices section says whether a backup code exists and lets it be made and withdrawn, and a sign-out that is the **last** device asks first, naming the ways back (`manage_view.dart` `lastDeviceWarning`/`confirmLastSignOut`; a device list that could not be read asks too). An album edit is ended by Cancel (the `×` of the edit mode's app bar, and `Escape`), which asks "Discard the changes to this album?" while the buffer is dirty and then re-fetches the album, and every way out of an album — the way up, the home button, a listing tile above, the system's or the browser's back button, a deep link — passes one guard in the router (`VAlbumRouterDelegate.leaveAlbum`, the mounted album registers its `AlbumContentState.confirmLeave` per path), which drops a clean edit session silently and asks "Save the changes to this album?" (Save / Discard / Stay) for a dirty one (#99). Navigation keeps one page per route level (#93): `VAlbumRouterDelegate.levelsOf` turns the route into the page stack (album → image / alternatives → member), so ascending pops and finds the album still mounted, with its state, its scroll offset and its thumbnails held live (each tile keeps its own listener on the image stream, which a covered page's `TickerMode` would otherwise drop), and a tile decodes its thumbnail at the height it is drawn at (`resizedThumbnail`, `ResizeImage` at display height × device pixel ratio, never upscaled). On the web the server screen builds no address section and no camera-roll section — a browser talks only to the server it was loaded from and has no camera roll — and names that server in one line instead (`ServerSettingsScreen.isWeb`, defaulting to `kIsWeb` and injected by widget tests, #90). The caller's role comes from one `?type=auth` per client (`caller.dart`, `CallerScope`) and hides at a guest's own root what the server refuses by role.

## Gotchas

- **Model classes are generated.** `image-server-shared/src/main/java/.../shared/model/model.proto` is the source of truth; the msgbuf-generator Maven plugin regenerates the model on every build. Edit the `.proto`, never the generated output. This generates **both** the Java model classes **and** the Dart file `valbum_ui/lib/resource.dart` (see the `option DartLib=...` line in `model.proto`) — `resource.dart` is generated, so don't hand-edit or reformat it.
- **Java 21** (`maven.compiler.release` in the root pom). The former 1.8 setting was a GWT constraint and is gone; a JDK 21 is required to build (the Maven enforcer checks it).

## Conventions

- Commit directly to `master` and push after every reviewed, gated change (see the `valbum-workflow` skill). Feature branches and PRs are for external contributors, not for the working session.
